services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: harakapay
      POSTGRES_PASSWORD: harakapay
      POSTGRES_DB: harakapay
    ports: ["5433:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]

volumes:
  pgdata:
